import "./index.css";

const PlacementAssistance = () => {
  return (
    <div className="placementAssistance-cont">
      <h1 className="main-heading">
        100% Plac<span className="special-style">ement</span> Assistance
      </h1>
      <p className="headings">Resume Building</p>
      <p>
        To attach more than one shadow to an element, add a comma-separated list
        of shadows see "Try it Yourself" example below
      </p>
      <p className="headings">Career Councelling</p>
      <p>
        To attach more than one shadow to an element, add a comma-separated list
        of shadows see "Try it Yourself" example below
      </p>
      <p className="headings">Jobs</p>
      <p>
        To attach more than one shadow to an element, add a comma-separated list
        of shadows see "Try it Yourself" example below
      </p>
    </div>
  );
};

export default PlacementAssistance;
